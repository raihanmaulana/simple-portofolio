/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com", "github.com"] // Tambahkan domain yang diperlukan
  }
};

export default nextConfig;
